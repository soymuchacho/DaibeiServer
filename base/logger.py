#coding=utf-8
import logging

logger = logging.getLogger("DaibeiServer.custom")


def log_write(level,args,*kw):
    	#str = args % kw
    	#if level == 'debug':
		#logger.debug(str)
    	#elif level == 'info':
        #	logger.info(str)
    	#elif level == 'error':
		# 	logger.error(str)
    	#elif level == 'warning':
        #	logger.warning(str)
    	#else:
        #	logger.critical(str)
		return None
